<%= bx_title %>

<%= bx_tabs params[:tab] %>

<div class="tab-content">
  <%= error_messages_for @form %>
  <%= labelled_form_for :form, @form, :url => project_bx_table_group_path(@project, @table_group), :method => :put do |f| %>
    <%= render :partial => "form", :locals => { :f => f, :form => @form, :databases => @databases } %>
    <%= f.submit l(:button_update) %>
    <%= link_to l(:button_cancel), project_bx_table_defs_path(@project) %>
  <% end %>
</div>
